# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Firebase deploy cache
.firebase/

# Local briefs and mockups (not part of the app)
CHAT_FEATURE_PROMPT.md
chat-mockup.html

# Secrets — local only, never commit.
# (Already matched by the broad *.local rule above; listed explicitly so the
#  intent is obvious. Holds ANTHROPIC_API_KEY, USDA_API_KEY, MCP_SHARED_SECRET
#  for the Functions emulator.)
functions/.secret.local
